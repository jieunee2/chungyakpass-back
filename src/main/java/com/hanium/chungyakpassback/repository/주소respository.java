package com.hanium.chungyakpassback.repository;

import com.hanium.chungyakpassback.domain.entity.input.주소;
import org.springframework.data.jpa.repository.JpaRepository;

public interface 주소respository extends JpaRepository<주소, Long> {
}
